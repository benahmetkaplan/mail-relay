{
    "name": "benahmetkaplan/mail-relay",
    "description": "A lightweight PHP mail relay for sending emails through an SMTP server via a simple HTTP API.",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "phpmailer/phpmailer": "^7.1",
        "vlucas/phpdotenv": "^5.7"
    },
    "autoload": {
        "psr-4": {
            "MailRelay\\": "src/"
        }
    }
}
