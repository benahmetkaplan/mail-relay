# Defense in depth: storage/ must never be web-accessible (rate-limit state, error log).
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
